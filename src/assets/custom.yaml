files:
    # Runs before `npm install` in '50npm.sh'
    "/opt/elasticbeanstalk/hooks/appdeploy/pre/60custombuild.sh" :
        mode: "000775"
        owner: root
        group: users
        content: |
            #!/bin/bash

            <% for(var key in instructions) { %>
            <%- instructions[key] %>
            <% } %>
